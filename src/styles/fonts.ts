import { Cutive_Mono, Cute_Font, Fira_Sans } from 'next/font/google';

export const cutiveMono = Cutive_Mono({ subsets: ['latin'], weight: '400' });
export const cuteFont = Cute_Font({ subsets: ['latin'], weight: '400' });
export const firaSans = Fira_Sans({ subsets: ['latin'], weight: '400' });
